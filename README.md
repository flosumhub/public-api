# Public API Swagger Repository

This repository stores OpenAPI (Swagger) specifications for public APIs used across multiple applications.

---

## 📁 Repository Structure

Each application has its own directory containing its API specification:

```text
.
├── backup/
│   └── swagger.json
├── devops/
│   └── swagger.json
├── data-migrator/
│   └── swagger.json
├── global-settings/
│   └── swagger.json
└── README.md
